@page

    <div class="text-center">
        <h1 class="display-4">Регистрация</h1>
        <br />
        <form id="signupForm">

            <input type="text" name="username" placeholder="username" />
            <input type="email" name="mail" placeholder="mail" />
            <input type="password" name="pass_word" placeholder="pass_word" />

            <button id="btn-all">Зарегестрироваться </button>
        </form>
    </div>

    <script>
        $(function () {
            // Обработчик события нажатия кнопки - весь список
            $('#signupForm').on('submit', function (e) {
                e.preventDefault();

                const formData = Object.fromEntries(new FormData(e.target).entries());

                $.post('/api/auth/signup', formData, function (data) {

                }, 'json');

            });

            /*   $('#btn').on('click', function () {
                   $.get('/api/movies/' + $('#MovieId').val(), function (data) {
                       $('#result').empty()
                       $('#result').html(data.Title +
                           '<br />' + data.Genre +
                           '<br />' + data.ReleaseDate);
                   }, 'json');
               });

               $('#btn-delete').on('click', function () {
                   $.ajax({
                       type: 'Delete',
                       url: '/api/movies?id=' + $('#MovieId').val()
                   });
               });*/
        });
    </script>